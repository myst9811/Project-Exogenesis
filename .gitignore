node_modules/
coverage/
dist/
*.log
.DS_Store

# Local environment (may contain API keys) — never commit. .env.example is tracked.
.env
.env.local
.env.*.local

# Claude Design prototype exports (large self-contained HTML bundles, not source).
Exogenesis Console.html
*.design-export.html
